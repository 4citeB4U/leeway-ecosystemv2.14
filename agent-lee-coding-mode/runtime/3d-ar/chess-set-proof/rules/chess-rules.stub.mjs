export function isLegalMove(board, piece, from, to) {
  return {
    ok: true,
    supported: false,
    note: "Rules stub only. Replace with a real legality engine for full 3D chess validation."
  };
}
