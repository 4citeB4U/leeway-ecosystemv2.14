# LeeWay IDE Single Canvas Assistant Entry

All external assistant bodies working in this directory must first obey the workspace root `AGENTS.md` and `000-URGENT-LEEWAY-ASSISTANT-LAW/READ-FIRST.md`.

Before IDE work that renders Runtime Fabric, terminal, device, command, model, voice, or vision surfaces, verify that the Runtime Fabric requirement register exists and read it:

- `../Leeway Runtime Fabric/reports/full-model-and-omni-terminal-fabric-system-requirements.md`

The IDE consumes Runtime Fabric truth endpoints and renders them as single-canvas nodes. It must not create fake terminal panes, fake terminal sessions, fake devices, fake hardware data, fake model readiness, or fake receipts.
